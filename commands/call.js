const Discord = require("discord.js");

module.exports.run = async (client, message, args) => {

message.author.send("you can chat directly with bot owner in this channel\nplease make sure you will help!!")

}

module.exports.help = {
    name: "call"
}